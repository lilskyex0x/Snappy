<h1>Welcome to My Application</h1>
<p>This is the splash screen.</p>
<% if user_signed_in? %>
<%= button_to 'Sign out', destroy_user_session_path, method: :delete, form: { data: { turbo_frame: "_top" } } %>
<% else %>
  <%= button_to 'Sign in', new_user_session_path %>
<% end %>